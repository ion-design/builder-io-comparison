export interface NavItemProps {
	icon: string;
	label: string;
	isActive?: boolean;
}

export interface PlaylistCardProps {
	image: string;
	title: string;
	artist?: string;
}

export interface SectionHeaderProps {
	title: string;
	subtitle?: string;
}
